# Rust
/target/
**/target/
*.rs.bk
# Cargo.lock IS committed — the workspace ships a binary crate (cli), so we pin deps.

# Swift / Xcode
.build/
*.xcuserstate
xcuserdata/
DerivedData/
*.xcframework      # generated from the Rust core; rebuilt by `make ios`

# WASM build output
/wasm/pkg/

# macOS
.DS_Store

# editors
.idea/
.vscode/
*.swp

# the original demo archive — the source is unpacked under prototype/ instead
maak-lang-interactive-demo.zip
